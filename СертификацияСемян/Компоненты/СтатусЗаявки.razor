@Текст

@code {
    [Parameter]
    public int Значение { get; set; }

    public string Текст => Значение switch
    {
        0 => Глобальные.СтатусЗаявки_Создана,
        1 => Глобальные.СтатусЗаявки_ВРаботе,
        2 => Глобальные.СтатусЗаявки_Рассматривается,
        _ => $"Неизвестное значение {Значение}"
    };
}
